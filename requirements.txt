# Core — needed for `run --mock --scorer keyword` (no API key required).
requests>=2.31
PyYAML>=6.0

# Uncomment below if you are using Anthropic as your LLM provider
# anthropic>=0.40

# Tests
pytest>=8.0
